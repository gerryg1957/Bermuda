MoyoDB progress report — 2 August 2026


Purpose

MoyoDB is being developed as a native replacement for MoyoGo Studio: a database of professional Go-games supporting large collections, incremental imports, game replay, exact-position indexing and visual pattern search.

The project is written primarily in Rust. The graphical application uses Qt 6, QML, Kirigami and CXX-Qt.

Core database work completed
SGF parsing and main-variation extraction.
Support for setup stones, captures, passes, simple ko and non-alternating move sequences.
Compact binary move-file storage.
Canonical SHA-256 game identities, independent of metadata.
SQLite project structure containing games, sources, metadata and indexes.
Duplicate-game detection with additional source records retained.
Single-file and recursive directory imports.
Import progress reporting, timing, summaries and error logs.
Incremental position-index construction and resumption.
Exact-position fingerprint lookup.
Database-backed game catalogue with preferred metadata.
Pattern extraction and matching.
Pattern searches over one game or an entire project.
Correct handling of board edges and corners during pattern matching.
Regression tests covering edge and corner constraints.
Graphical application completed so far
Qt/Kirigami application connected to the Rust core through CXX-Qt.
Project game catalogue.
Loading and replaying database games.
External SGF loading.
Move slider and first/previous/next/last navigation.
Editable new positions with black, white and erase tools.
Rectangular pattern selection directly on the board.
Pattern searching from:
a database game position;
an externally loaded SGF;
an artificially created position.
Search-results tab with:
game metadata;
number of matches per game;
total matching games;
total raw occurrences.
Selection of a search result opens the matching game.
Every occurrence for the selected game is retained.
Previous-match and next-match navigation.
Match rectangle displayed at the corresponding board location.
Correct distinction between several matches on the same move at different locations.
Conversion between QML top-origin coordinates and core bottom-origin coordinates.
Previous results are invalidated when:
a new search area is started;
the editable source position changes;
a different source document is selected.
Pattern-selection mode now exits correctly after a drag or clear operation.
Repeated selection, clearing and reselection now works with a single click.
Validation completed

The following checks have passed:

cargo fmt --all --check
cargo test
cargo clippy --all-targets --all-features -- -D warnings
git diff --check

Manual testing has covered catalogue games, editable positions, external SGFs, repeated pattern selections, edge-constrained searches, result selection and match navigation.

Current known limitations

Raw occurrence counts. A pattern is currently counted at every game position where it exists. When moves are played elsewhere, an unchanged local pattern can therefore produce many consecutive matches. A future refinement should collapse consecutive matches at the same board location into one “match episode”.

Synchronous searches. The interface displays a searching state, but the core search itself remains synchronous. Very large databases may temporarily block the interface. A later implementation should run searches in a worker thread and support cancellation and real progress reporting.

Distribution and packaging. The application currently builds from source. It does not yet have Flatpak, AppImage, RPM or Debian packaging.

Testing data. A small distributable SGF corpus or ready-made test project is not yet included, making first-time testing less straightforward than it should be.

Cross-platform testing. Development has principally taken place on openSUSE Tumbleweed. GNOME and other desktop environments need documented testing.

Recommended next steps
Commit the current GUI pattern-search work.
Add build and testing instructions to the repository.
Create a small redistributable test corpus.
Test on GNOME under both Wayland and X11/XWayland.
Test search performance on a substantial real database.
Collapse persistent consecutive matches into match episodes.
Move searches to a background worker.
Add filtering, sorting and richer metadata controls.
Consider the planned separate corpus for users’ personal games and recurring-mistake analysis.

## Update — 3 August 2026: Database workflow foundation

### Purpose

This milestone prepares MoyoDB's database-management operations for use
from the Qt GUI without invoking or parsing command-line programs.

The GUI will call stable Rust library APIs directly. Long-running work
will run outside the GUI thread and report structured progress back to
QML.

### Core import operation

Directory import has been refactored into a reusable library operation.

It now provides:

- a discovery stage that counts SGF files before import;
- determinate progress during import;
- the current SGF path;
- counts for imported games, added sources, duplicates, skipped games
  and errors;
- elapsed time and processing rate;
- an import error-log path;
- cancellation between SGF files;
- a final completed or cancelled outcome.

Cancellation does not roll back SGFs already imported. Each completed
file remains safely committed, and the database remains internally
consistent.

The existing command-line `import-dir` operation now uses this shared
library API rather than maintaining a separate implementation.

### Position-index operation

Position-index building has also been moved into a reusable library
operation.

It now provides:

- the active index version;
- the number of games awaiting indexing;
- processed and successfully indexed game counts;
- the number of indexed positions;
- error count and error-log path;
- elapsed time and processing rate;
- cancellation between games;
- completed and cancelled outcomes.

Indexing one game is transactional. A cancelled build therefore leaves
a valid partial index which can be resumed by running the operation
again.

The command-line `build-position-index` command now uses the same
library implementation intended for the GUI.

### Qt background worker

A new QML-facing `DatabaseOperationModel` has been implemented with
CXX-Qt threading support.

It supports three operations:

1. create a database, import an SGF directory and optionally build the
   index;
2. add games to an existing database and optionally update the index;
3. update the position index independently.

The model exposes structured properties for:

- operation and stage names;
- status and error messages;
- the resulting project path;
- current file or game;
- import progress and summary values;
- index progress and summary values;
- elapsed time and rate;
- cancellation state;
- import and index error logs.

Long-running work runs on a background Rust thread. Progress updates are
throttled to approximately ten updates per second so that large imports
and index builds do not overwhelm the Qt event queue.

An atomic cancellation token is shared with the worker. An operation ID
prevents delayed messages from an older operation from updating the
model after a newer operation has begun.

### Verification

The following checks succeeded:

- Qt crate compilation;
- 90 library unit tests;
- 5 core integration tests;
- 2 pattern-search integration tests;
- Qt-package Clippy with warnings denied;
- workspace-wide Clippy with warnings denied;
- formatting and `git diff --check`.

The remaining linker-selection and Qt/GCC SFINAE messages are generated
by the local C++/Qt build environment and do not represent Rust or
CXX-Qt compilation failures.

### Current stopping point

The reusable core operations and the Qt background worker are
implemented and compile-verified.

The worker is not yet instantiated or used by QML. No database menu,
database forms or operation-progress dialog has yet been added.

Development should resume with:

> the GUI layer: a Database menu, create/add-games forms, and a single
> progress dialog driven by the background worker.

At the last recorded repository check, the Qt worker changes had not
yet been shown as committed. Confirm the repository state with
`git status --short` when development resumes.

## Work plan from the current stopping point

### Stage 1 — Instantiate the worker in QML

Add one `DatabaseOperationModel` instance to `Main.qml`.

It should be the single authoritative source for database-operation
state throughout the application.

Do not duplicate its progress or status in several parts of the
interface.

### Stage 2 — Add the Database menu

Add a top-level **Database** menu containing:

- **Create Database…**
- **Add Games…**
- **Update Position Index**
- **Cancel Current Operation**, enabled only while work is running

Actions that require an open project must be disabled when
`projectPath` is empty.

Database actions must also be disabled while another database operation
is active.

### Stage 3 — Create Database form

Add a form that collects:

- database name;
- destination project directory;
- SGF source directory;
- source name;
- source version;
- whether to build the position index after import.

The form should validate required values before calling
`createDatabase()`.

The existing project manager must continue to refuse to overwrite an
existing path.

### Stage 4 — Add Games form

Add a second form for the currently open project.

It should collect:

- SGF source directory;
- source name;
- source version;
- whether to update the position index after import.

The current `projectPath` should be shown clearly but should not require
the user to select the project again.

### Stage 5 — Shared operation-progress dialog

Create one progress dialog used by creation, import and indexing.

It should show:

- a human-readable operation title;
- the current stage;
- indeterminate progress during SGF discovery;
- determinate progress during import;
- determinate progress during indexing;
- the current file or game where useful;
- imported, duplicate, skipped and error counts;
- indexed game and position counts;
- elapsed time and rate;
- a Cancel button while work is active.

After completion, cancellation or failure, the same dialog should show
the final summary rather than opening a separate result window.

The dialog must accurately distinguish:

- completed;
- cancelled after partial progress;
- failed.

It must not imply that cancellation rolls back already completed import
or indexing work.

### Stage 6 — Refresh the active project

After successful database creation:

- assign the returned project path to `root.projectPath`;
- load the new game list;
- clear stale game, pattern and search selections;
- update any project-dependent controls.

After adding games:

- reload the game list;
- clear search results that may no longer represent the complete
  database;
- retain the current project path.

After index completion:

- leave the current game list and board state intact unless reloading is
  technically necessary.

### Stage 7 — Error handling

Present validation and operation errors in the GUI.

When an error log was written, show its path in the final summary.

Errors importing individual SGFs should not be presented as though the
whole operation failed when the operation otherwise completed.

### Stage 8 — Runtime acceptance tests

Test first with a small temporary project.

Required cases:

1. create a new database and import a few SGFs;
2. create and import with automatic index building;
3. add games without updating the index;
4. add games and update the index;
5. update an already current index;
6. cancel during SGF discovery;
7. cancel during import;
8. cancel during indexing;
9. attempt to create a database in an existing path;
10. import duplicates and verify the summary;
11. import an invalid SGF and verify the error summary and log;
12. verify that the interface remains responsive throughout.

After the small tests, repeat import, indexing and cancellation against
a substantial real collection.

### Stage 9 — Commit the GUI database milestone

Run:

- `cargo fmt --all`;
- `cargo check -p moyodb-qt`;
- all tests;
- Qt-package Clippy with warnings denied;
- workspace Clippy with warnings denied;
- `git diff --check`.

Then commit the complete GUI database workflow as one coherent
milestone.

## Work following the database GUI milestone

Once database creation, import and indexing work reliably from the GUI,
the next priorities are:

1. improve startup and project-opening behaviour, including a welcome
   view and recent projects;
2. run a release-mode benchmark against the full professional-game
   database;
3. profile and optimise project-wide pattern searches;
4. collapse repeated matches in unchanged consecutive positions into
   meaningful pattern episodes;
5. add continuation and frequency visualisations;
6. consider KataGo-based analysis only after the native database and
   search workflow is mature.
